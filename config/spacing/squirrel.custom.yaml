# Merge these keys into your existing patch; this file is an opt-in example.
patch:
  spacing_context/enabled: true
  spacing_context/mode: off
  spacing_context/apps/com.apple.TextEdit/mode: verified
  # Enable further apps only after checking their IMK document support.
  # spacing_context/apps/com.google.Chrome/mode: verified
  # spacing_context/apps/com.microsoft.VSCode/mode: verified
  # events_only skips document reads; external edits may be missed.
